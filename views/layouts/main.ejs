<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda de Camisetas</title>
    <!-- Si tenés un archivo CSS, iría linkeado acá -->
</head>
<body style="margin: 0; padding: 0; font-family: Arial, sans-serif;">

    <!-- 1. El Header fijo para todas las páginas -->
    <%- include('../partials/header') %>

    <!-- 2. Acá adentro va a aparecer mágicamente el contenido de index, product, cart, etc. -->
    <%- body %>

    <!-- 3. El Footer fijo para todas las páginas -->
    <%- include('../partials/footer') %>

</body>
</html>